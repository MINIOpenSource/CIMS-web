"use client";
import JsonResourceEditor from "@/components/JsonResourceEditor";

export default function ComponentsPage() {
    return (
        <JsonResourceEditor
            resourceType="Components"
            title="组件设置管理"
            subtitle="管理 ClassIsland 组件配置 (Components)"
        />
    );
}
